[preferred]
# TideWM only implements the ScreenCast backend interface itself (see
# src/screencast/portal.rs). Everything else falls back to gtk, which is
# what most distros already have installed for the file chooser and friends.
default=gtk
org.freedesktop.impl.portal.ScreenCast=tidewm
